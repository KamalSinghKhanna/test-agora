look at these component properly, in the jsx popover -> popovertrigger -> text has shown the transcript text, and they are using llms to generate that text, and after taat you can select some text and create a tag about that selected part, when you enter the tag and enter it will highlight that part of the selected text, now the problem is when we select the text browser by default give is a skyblue color and a popover appears, when you click on that popover to input the tag name the selected sky blue color goes away, now we want that color to be stayed, until we put the tag name and ciclick the enter the button to add the new tag, and also f the text has already highlighted and if we select the text once agin it should not remove the color